#include "../../inc/libregex/src/libregex.h"
#include <criterion/criterion.h>
#include <criterion/internal/assert.h>

Test(match, float)
{
    t_re **re;

    re = re_new(RE_FLOAT);
    cr_assert(match(RE_))



}
